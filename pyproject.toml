[project]
name = "amelia-pt-exams"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "datasets>=5.0.1",
    "inspect-ai>=0.3.261",
    "modal>=1.3.5",
    "openai>=3.6.0",
]

[dependency-groups]
dev = [
    "ruff>=0.16.5",
]
